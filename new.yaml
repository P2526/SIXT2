swagger: "2.0"
info:
  title: "SSRF Testing"
  version: "1.0.0"
paths:
  /test:
    get:
      parameters:
        - in: query
          name: url
          type: string
          required: true
          description: "URL to fetch (SSRF test)"
          default: "https://996q5r8xqfl1xyqeyj9zrrg82.canarytokens.com"
      responses:
        200:
          description: "Successful SSRF test"
          schema:
            type: string
